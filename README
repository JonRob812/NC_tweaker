concept is to drag a NC file onto a python script and have a command prompt
that ask user what mods to do

mods to file:
-rotate
-translate
-tool number change
-offset change
-split

save output file - ask if default file name ok


objects:
    % com start/end
    comment
    comment with tool
    safety codes
    clamp / unclamp
    table rotation
    rapid
    linear
    arc
    canned cycle
    location


(?<!\(.)([A-z]-?\d*\.?\d+\.?) neg look behind


def rotate_(point, angle, center_point):
    angle = math.radians(angle)
    x, y = point
    center_x, center_y = center_point

    sin = math.sin(angle)
    cos = math.cos(angle)
    temp_x = x - center_x
    temp_y = y - center_y
    rotated_temp_x = temp_x * cos + temp_y * sin
    rotated_temp_y = (temp_x * -1) * sin + temp_y * cos

    rotated_x = round(rotated_temp_x + center_x, 4)
    rotated_y = round(rotated_temp_y + center_y, 4)
    return rotated_x, rotated_y